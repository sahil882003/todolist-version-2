<%- include('header') %>
    <div class="box" id="heading">
        <h1>
            <%= listtitle%>
        </h1>
    </div>
    <div class="box"> 
        
        <% if(route==="/"){ %>
        <% var deleteroute="/today/delete" %>
        <% } else { %>
        <% var deleteroute=route+"/delete"; %>
        <% } %>
        
        <% for(var i=0;i< items.length ;i++){ %>
            <form action=<%= deleteroute %> method="post">
            <div class="item">
                <input type="checkbox" onChange="this.form.submit()" name="checkbox" value=<%= items[i]._id %> >
                <p>
                    <%= items[i].item.name %>
                </p>
            </div>
        </form>
            <% } %>
                <form class="item" action=<%= route %> method="post">

                    <input type="text" name="newitem" placeholder="new item" autocomplete="off">
                    <button value="submit" type="submit">+</button>

                </form>
    </div> -->
    <%- include('footer') %>

